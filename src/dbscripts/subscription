create table church.subscription (
id int AUTO_INCREMENT PRIMARY KEY ,
familyId int,
serialId int,
amount double,
subMonth VARCHAR(24),
subYear int,
paidDate date,
createdAt datetime,
updatedAt datetime
);

ALTER TABLE church.members 
MODIFY column familyId  VARCHAR(255)
